; Compiles the documentation site to JavaScript ES modules. Vite consumes the
; emitted entry from _build and publishes the static site from website/.
(melange.emit
 (target output)
 (modules app)
 (libraries reativa)
 (preprocess
  (pps reativa.mlx_ppx melange.ppx))
 (module_systems es6))
