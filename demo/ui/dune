; Compiles the JSX-backed view demo to JavaScript ES modules under
; demo/ui/output/. Bundle the emitted entry for the browser with esbuild; see
; the npm "demo" script.
(melange.emit
 (target output)
 (libraries reativa)
 (preprocess
  (pps melange.ppx reativa_jsx_ppx))
 (module_systems es6))
